import { Root, Pre } from "./code"
import { Tab, TabContent, TitleBarContent } from "./title"

const components = {
  Pre,
  Root,
  TitleBarContent,
  Tab,
  TabContent,
}

export default components
